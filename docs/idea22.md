So, I was just thinking, so I'm just documenting some of the design decisions which I had initially and how they evolved over time. So, from the very beginning I always knew it was something, the platform was something to do with running agents in a sandbox. That was the whole idea. So, you can have agents running in a sandbox and they can work on tasks that you assign them whilst you do something else. So, instead of you constantly monitoring them in an IDE, they do their work in the background in a sandbox environment and then once they are done they do a pull request for when a feature is complete. So, that was the whole idea. And this was heavily inspired by the Ralph Wiggum approach where it's called the Ralph Loop where you have a code agent in a sandbox and you have a script which prompts the code agent to keep on running until task completion. So, when this hackathon came up I've always wanted to implement something like this. So, it is a good opportunity to implement something like this because I believe the Gemini 2 kit and everything I needed, it has the CLI, the reasoning model and a faster, lighter model. That is Gemini 3 Flash. So, the plan is aligned. So, initially it would be a platform where you move from being an engineer level where you are writing most of the code and you have AI assist you writing the code and you're doing everything in the IDE to more of like a technical lead where the code agents become engineers in and of themselves and it's sort of like a team effort. So, in this case you would not spend much time at all in the IDE. You would be simply assigning tasks, reviewing code and asking for changes to code agents. So, something like that, that would be the system. So, on the learning page I always like to break down things from the user story perspective. That's how I always come up with the product. It's what should the user experience be like first and then we work back from that. So, we're not trying to show then engineering feeds and best practices into your product. It's actually the other way around. We figure out the user experience, what it's supposed to be like and then we work back and reverse engineer to what that would mean at a technical implementation level. So, that's what I've always done and I think it makes for a good product. So, yeah. So, the idea, the user journey would be you had an idea, right? You have a project which you want to start and then you briefly explain it and then you choose your text, your framework you want to use and you have all of these flags. You choose your database provider and you choose your framework, you choose everything and then you could also select the team you wanted to use and all of that. So, it was really kind of, it would allow you to do a lot of configuration on project creation.


And then you would go ahead and create the project and that would initialize a tab repo. And then you would have these agents, you have these tasks for that specific project broken down into atomic actionable to-dos, which a code agent can take up and do. So that kind of evolved from having a to-do list, markdown file in a sandbox and you have a code agent constantly running until it finishes it to having more of like a project management approach where the tasks are declaratively built and initiated from the user interface where you have your Kanban board and you can click initialization of a task, you can approve or commit and review the code or within the platform. And initially we wanted to have reviews in a separate user interface where you would probably do a PR on GitHub. And then once approved, we would have a webhook we would be listening to to revalidate our UI so that the next task can come through after the first one has been approved. And so that kind of changed a bit because we decided to integrate the code review part into the platform itself. So those were some of the changes we made.

Okay, so we ran into a couple of major blockers. The first one is that the Gemini CLI itself, we were having issues running it. It was not starting properly. So it turned out it was our runtime because initially we were using the bun runtime as the base in our sandbox. So we had to switch to a different runtime. We then used the node and that worked. So bun was just our CLI too. So that resolved the first error. And we also had another second error where we had no network access in the sandbox. So the CLI could not run properly. So that took a lot of time to debug and finally figure out that the reason why the CLI is not starting is because it is not able to do a network call to the Google service. So that was another major block. And that took a long amount of time to resolve because we went through a lot of iterations trying to resolve this. And yeah, those were some of the major blockers, but we eventually got over them.

The Gemini skills implementation is not stable, it causes unexpected behavior in some cases, so you have to add some guardrails to prevent it from tampering with the dot Gemini directory.

And another issue we ran into was the shadcn and some of the long-running commands were not working properly. The Gemini CLI was not able to run some long-running commands like build commands, starting the dev server. Commands like that were given errors. And so we had to do some work around and we just decided to make it run some TSC checks. That was all the validation we had to do. And it was not able to install some of the shadcn components via the CLI. So what we ended up doing is to have the template itself, have all of the components already, and we just build from those. And then the project will use whatever components it needs because all of them are already in the project. And then, yeah, that was those were some of the challenges I faced.


So we ended up using the upstash real-time for our events because in a deployed environment, we were having issues with our SSE custom implementation, so we decided to use Redis server via the upstash real time SDK, and it started working fine, now it's working perfectly, so that's what we now use for our real time events. Our updates implementation has been changed to use that, and it's now much more reliable than what we had with the SSC implementation. In a deployed environment, it wasn't working at all.


So future points of improvements would probably be to make the agents more autonomous, in that they don't need to be so platform controlled, because currently scaling is directly proportional to, is directly proportional to compute, which is not tenable. We'd want the agents to be more autonomous, where we move most of the execution, the avia to the set boxes, instead of encapsulating everything within our platform where we have an orchestrator. So that would probably be the future development phases. So those are some of the things you can improve on as we going forward.

So actually I faced some issues regarding the stability of other templates and we decided to just go with reactivit because that was the most stable. We don't want to risk supporting frameworks which could potentially break when you try to use them. And so we're now just supporting which react it's more stable and I think for the time and the scope of the project it makes sense. So that's what we ended up going with to make sure the platform is robust and actually works.