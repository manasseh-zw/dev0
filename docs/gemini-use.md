So we are using Gemini quite extensively. So the first, we're using it firstly for the voice input. We are sending an audio attachment to the Gemini 3 flash model and it's returning a transcription. So you use it the very first time you use the app, you can use the voice input feature and that will be Gemini powered by Gemini 3 flash specifically. It has very good audio understanding. And next we hand off that input idea which you had and we give it to the preview agent. So this agent will essentially assign draft app a name relevant to your idea and description and it will select the text that it recommends. So that's what the preview agent is doing. So after the preview agent is done, it's also powered by Gemini 3 flash. We hand over what the preview agent has generated. You can also edit it in the tag stack. We hand it over to the planner agent. So the planner agent is the one which is going to generate and decide the relationship between all of the tasks. The planner agent is powered by Gemini 3 Pro since it's a foundational step which requires understanding of how code actually works. So it's the one which will generate the tasks which need to be executed and it will assign different tasks to either Gemini 3 Pro or Gemini 3 flash depending on the complexity of the task. And then it also decides the dependencies between the tasks like which task should go before which task and then the phases of the task as well are all done by the planner agent.

So after the planning agent generates all of their tasks with the phases and assigned to the different models, flash or pro depending on complexity. We also have the Gemini CLI which is running in our sandbox environment. So we pass that task to Gemini and depending on what was specified on the task we let Gemini CLI run in the sandbox using that specified model. So if it's 3 pro that's what will be running in the Gemini CLI in our sandbox executing that task and when it is done we open up a pull request which we push to github. So Gemini is being used quite extensively and the Gemini 3 model family is quite good. It's able to handle all of this and it's very versatile and we're able to use it in a lot of places.